---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mentor
  namespace: mentors
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mentor
  template:
    metadata:
      labels:
        app: mentor
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: demo
          image: ghcr.io/claivent/demo6-backend:latest
          ports:
            - containerPort: 9111
---
apiVersion: v1
kind: Service
metadata:
  name: mentor-backend
  namespace: mentors
spec:
  selector:
    app: mentor
  ports:
    - port: 80
      targetPort: 9111
