# ArgoCD repository secret — TEMPLATE ONLY, do not store real tokens here.
# Apply manually with the real token:
#
#   kubectl create secret generic argocd-repo-bacprac2026 \
#     -n argocd \
#     --from-literal=type=git \
#     --from-literal=url=https://github.com/claivent/bacprac2026-app.git \
#     --from-literal=username=claivent \
#     --from-literal=password=<GITHUB_PAT> \
#     --dry-run=client -o yaml | \
#   kubectl label --local -f - argocd.argoproj.io/secret-type=repository \
#     --dry-run=client -o yaml | \
#   kubectl apply -f -
#
# Token requirements: repo scope (read/write), packages scope
# Token name on GitHub: VPS-claude-contabo
# Expires: 2027-04-07
apiVersion: v1
kind: Secret
metadata:
  name: argocd-repo-bacprac2026
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
type: Opaque
stringData:
  type: git
  url: https://github.com/claivent/bacprac2026-app.git
  username: claivent
  password: <GITHUB_PAT>
